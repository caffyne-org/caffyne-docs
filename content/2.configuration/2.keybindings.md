---
title: Keybindings
description: Interface with the desktop shell over IPC using fabric-cli commands.
navigation:
  icon: i-lucide-keyboard
---

Caffyne Shell deliberately delegates keyboard layout and shortcut assignments to your host window manager or compositor. Applets and menus are toggled smoothly via an Inter-Process Communication (IPC) layer using `fabric-cli`.

## IPC Syntax Example

Here is how you can map shell toggles using **Niri** syntax configurations:

```ini [config.kdl]
Mod+Space { spawn "fabric-cli" "exec" "caffyne-shell" "bar_manager.toggle('Launcher')"; }
Mod+Q     { spawn "fabric-cli" "exec" "caffyne-shell" "bar_manager.toggle('Session')"; }
Mod+N     { spawn "fabric-cli" "exec" "caffyne-shell" "bar_manager.toggle('Notifications')"; }
```

---

## Available Applets

Pass any of the following identifier handles explicitly into `bar_manager.toggle('<Applet>')` to control component actions:

:::field-group

  ::field{name="Launcher" type="Applet"}
  Launches the searchable desktop application selector overlay.
  ::

  ::field{name="Settings" type="Applet"}
  Spawns the quick settings utility control interface dashboard.
  ::

  ::field{name="Notifications" type="Applet"}
  Reveals your timeline history database for active alerts.
  ::

  ::field{name="Clock" type="Applet"}
  Visualizes a real-time system clock layout utility widget.
  ::

  ::field{name="Calendar" type="Applet"}
  Expands into an interactive dashboard calendar tracker.
  ::

  ::field{name="Weather" type="Applet"}
  Pulls real-time climate telemetry updates for local regions.
  ::

  ::field{name="Media" type="Applet"}
  Direct interface engine with `playerctl` for media controls.
  ::

  ::field{name="Volume" type="Applet"}
  Manages your sound architecture and pulse/audio mixers.
  ::

  ::field{name="Wifi" type="Applet"}
  Interactive connection panel for network managing protocols.
  ::

  ::field{name="Bluetooth" type="Applet"}
  Scans, syncs, and drops connected bluetooth devices.
  ::

  ::field{name="Energy" type="Applet"}
  Tracks battery consumption and system power thresholds.
  ::

  ::field{name="Session" type="Applet"}
  Triggers immediate system logout, lock, reboot, or poweroffs.
  ::

  ::field{name="Calculator" type="Applet"}
  Spawns a swift algebraic computing overlay helper tool.
  ::

  ::field{name="Keyboard" type="Applet"}
  Allows on-the-fly cycling through global layout languages.
  ::

  ::field{name="Processes" type="Applet"}
  Pulls resource usage metrics for active tasks via `psutil`.
  ::

:::
